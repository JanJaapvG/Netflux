import { combineReducers } from "redux";
import HomePageReducerSlice from '../Store/HomePage/Reducer'

// combines all slices of data that will be loaded onto the homepage
export default combineReducers({
HomePageReducerSlice
});